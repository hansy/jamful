{
  "name": "@jamful/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run dev:setup && bun run migrate:local && wrangler dev",
    "dev:setup": "bun ./scripts/dev-setup.ts",
    "build": "wrangler deploy --dry-run",
    "build:production": "wrangler deploy --dry-run --secrets-file .env.production",
    "migrate:local": "wrangler d1 migrations apply JAMFUL_D1 --local",
    "migrate:production": "wrangler d1 migrations apply JAMFUL_D1 --remote",
    "deploy:production": "wrangler deploy --secrets-file .env.production",
    "deploy": "bun run build:production && bun run migrate:production && bun run deploy:production",
    "cf-types": "wrangler types",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@jamful/shared": "workspace:*",
    "jose": "^6.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.28.0"
  }
}
